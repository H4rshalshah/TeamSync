services:
  mongo:
    image: mongo:7
    container_name: teamsync_mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - teamsync_mongo_data:/data/db

volumes:
  teamsync_mongo_data:
